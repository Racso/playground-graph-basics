def get_graph_info(matrix):
    #matrix is the adjacency matrix of a graph. It's a list of lists. E.g. [[0, 1], [1, 0]].

    return 2, 1, 0, False #fix me: for the given graph, return: number of nodes, number of edges, number of loops, a boolean stating if the graph has parallel edges.
